#pragma once
#include "LinkedList.h"
#include <ctime>

using namespace std;

//TODO: именование не от глагола
void PrintList(List *);
int ReadValue();
bool ChoosePosition(List *);
bool CheckForInteger(string);